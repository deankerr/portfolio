<section aria-label="Other work">
	<p>other work</p>
	<slot />
</section>

<style>
	section {
		padding-block: 1.75rem 1rem;
		border-top: 1px solid var(--line);
	}

	p {
		color: var(--ink-faint);
		font-family: var(--font-mono);
		font-size: 0.72rem;
		font-weight: 500;
		letter-spacing: 0.16em;
		text-transform: uppercase;
	}
</style>
